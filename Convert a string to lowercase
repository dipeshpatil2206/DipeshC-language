#include <stdio.h>
#include <string.h>


int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);
    for(int i = 0; str[i]; i++)
        str[i] = tolower(str[i]);
    printf("Lowercase string: %s\n", str);
    
}
